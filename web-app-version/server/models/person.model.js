export default class Person {
  // name: string, currentFloor: number, dropOffFloor: number
  constructor(name, currentFloor, dropOffFloor) {
    this.name = name,
    this.currentFloor = currentFloor,
    this.dropOffFloor = dropOffFloor
  }
}
